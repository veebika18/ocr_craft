import cv2

def detect_text_regions(image):
    # Placeholder: real CRAFT should be implemented or loaded from PyTorch
    h, w = image.shape[:2]
    return [(10, 10, w - 20, h - 20)]  # Dummy region
